#include <iostream>
using namespace std;

int factorial(int n)
{
    int fact=1;
    int i;
    for(i=n;i>0;i--)
    {
        fact=fact*i;
    }
    return fact;
}
int main() {
    int n;
    cin>>n;
    cout<<"Factorial of "<<n<<" is "<<factorial(n);
	// your code goes here
	return 0;
}
